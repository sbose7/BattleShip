package JobProcessing;

/**
 * The kinds of work the job processing system knows how to handle.
 * Add new members here and pair each with a {@link Consumer} implementation.
 */
public enum JobType {
    HELLO,
    SAVE
}
